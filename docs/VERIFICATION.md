# 検証メモ

2026-09-18。公式バックエンド `7bfa7ba6c71f977c8f9117d0ddbb95a2151f08b8`、UI `a61b636305a25e3735aeccc934c32dc2fea350a4` を調査。

確認したAPI: `GET /api/v3/users/me`、`GET /api/v3/channels/{id}/messages?limit=1&order=desc`、`GET /api/v3/messages/{id}`、`PUT /api/v3/messages/{id}`。編集成功は204、本文上限はUnicodeコードポイント10,000文字。UIはAxiosで投稿し、成功時のみ入力をクリアします。

## 自動検証

- `npm test`: 9テスト。設定値、対象URL、時間境界、改行追記、所有者・チャンネル・ピン・古い投稿の除外、未知のpayload、Unicode文字数、追記後の時間窓、表示結合条件。
- ブラウザ: Playwright + ローカルHTTP API + Vue 3.5.13本番ビルド + Axios 1.8.4。専用Browserプラグイン未提供のためPlaywrightを使用。
- デスクトップ1280×800、モバイル相当390×844。Chrome headless。実サービスのDOM構造に合わせたテスト画面で実行。

検証フローは「テスト画面を開く → 送信/設定変更 → 編集リクエストと表示・入力欄の状態を確認」。

| 確認 | 結果 |
| --- | --- |
| ページ名・非空画面・フレームワークエラーなし | PASS |
| Vue本番モードで連続発言のヘッダーを畳む | PASS |
| 表示の無効化で復元、秒数変更で再判定 | PASS |
| Axios XHR: POSTからPUTへ変換、成功時入力クリア | PASS |
| PUT失敗: 入力保持、POSTで自動再送しない | PASS |
| 他人の割り込み後はPOST | PASS |
| Axios fetch adapterでもPUTへ変換 | PASS |
| 1回だけ新規投稿、その次は追記 | PASS |
| 読み取り失敗時は元のPOST | PASS |
| 複数タブ同時送信で両方の文章を保持 | PASS |
| 事前確認中のキャンセルで書き込みなし | PASS |
| 自分の追記を無効にすると通常POST | PASS |
| モバイル幅で設定操作・横にはみ出さない | PASS |
| 未処理JavaScriptエラー | 0 |

意図的な503エラーを試験しています。ネットワークの503表示は失敗ケースの想定内です。

## 未検証・制約

- ログイン済みの実traQ、実WebSocket通知、実添付アップロード、実サービスのスクロール位置保持。
- Tampermonkey経由の実インストール、Chrome Web Store審査、Firefox/Safari、ネイティブアプリ。
- 独自UIや将来のVue/CSS変更。表示結合アダプターは保守が必要です。
- 条件付き編集APIがないため、読み取りと書き込みの間に別端末から変更される競合は完全には排除できません。

実環境の受け入れ確認では、テストチャンネルで短い文章2件→1件への追記、他ユーザー介入→新規投稿、編集失敗時の入力保持、添付・スタンプ・未読区切り・スクロールを確認してください。
